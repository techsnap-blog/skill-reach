/* ============================================================
   article-promo.js
   記事ページの<body data-cat="...">を見て、該当カテゴリの
   記事だけに固定の広告/プロモブロックを自動挿入する。
   新しい記事を追加する際は、body要素にdata-cat属性を
   付けるだけで自動的に表示される（HTMLへの手動コピー不要）。
   現在カテゴリ別プロモは未設定。必要になったらここに追加する。
   ============================================================ */
(function () {
  'use strict';

  const cat = document.body.dataset.cat;
  void cat;
})();
